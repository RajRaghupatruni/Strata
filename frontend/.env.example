# Optional. Defaults to the local backend; include /api/v1.
VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1

# Set true when building a frontend for a PUBLIC_DEMO_MODE backend.
# This only adjusts the UI; the backend independently enforces read-only access.
VITE_PUBLIC_DEMO_MODE=false
